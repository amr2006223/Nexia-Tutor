package com.nexia.nexia.services;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.nexia.nexia.models.Lesson;
//import com.nexia.nexia.repositories.LessonRepository;
import java.util.*;

@Service
public class LessonsService {
    // @Autowired
    // private LessonRepository lessonRepository;

    // TODO: add a method to get all lessons
    // public List<Lesson> getAllLessons() {
    // // return new SomeData();
    // List<Lesson> lessons = lessonRepository.findAll();
    // // Map<String, Object> lessonsMap = new HashMap<>();
    // // for (Lesson lesson : lessons) {
    // // lessonsMap.put("lesson_id", lesson.getId());
    // // lessonsMap.put("lesson_Name", lesson.getLesson_name());
    // // }
    // return lessons;

    // }

    // Lesson getLessonById(Long id) {
    // Lesson lesson = lessonRepository.findById(id).orElse(null);
    // return lesson;
    // }

    // Lesson getLessonByName(String name) {
    // Lesson lesson = lessonRepository.findByLesson_name(name);
    // return lesson;
    // }
}
